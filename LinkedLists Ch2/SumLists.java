/* Given two nums represented as linked lists, where each 
node contains digits. Digits stored in reverse order, 
head = 1s place. Create a function that adds the two
nums and returns sum as linked list */ 


// INPUT (7 -> 1 -> 6) + (5 -> 9 -> 2) = (617 + 295)
// OUTPUT 2 -> 1 - > 9 (912)

class PartialSum {
	public LinkedListNode sum = null; 
	public int carry = 0; 


}


LinkedListNode sumLists(LinkedListNode l1, LinkedListNode l2) {


// check the length of the two numbers
// if one is shorter than other, pad it with zeros 


// leftover carry - insert to front of list
// otherwise return the linked list



// the carry is just another int that we have to keep track of
// carry = l1.data + 12.data + sum.carry / 10 

// value added to the list is all digits % 10 


// insertBefore + padList used to padd beginning of the shorter
// linked list with nodes of data = 0 
}






